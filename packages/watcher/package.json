{
  "name": "@algo-jit/watcher",
  "version": "0.1.0",
  "description": "File watching for algo-jit",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "vitest"
  },
  "dependencies": {
    "@algo-jit/instrumenter": "workspace:*",
    "@algo-jit/executor": "workspace:*",
    "chokidar": "^3.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.16",
    "typescript": "^5.3.3",
    "vitest": "^1.2.2"
  }
}
